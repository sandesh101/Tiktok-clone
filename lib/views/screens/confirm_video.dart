import 'package:flutter/material.dart';

class ConfirmVideo extends StatelessWidget {
  const ConfirmVideo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
